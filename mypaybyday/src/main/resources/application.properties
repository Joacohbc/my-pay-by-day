quarkus.datasource.db-kind=sqlite
quarkus.datasource.jdbc.url=${QUARKUS_DATASOURCE_JDBC_URL:jdbc:sqlite:mypaybyday.db}

# SQLite: keep pool small; WAL mode (configured via SQLiteConfig) handles concurrent reads
quarkus.datasource.jdbc.max-size=2
quarkus.datasource.jdbc.min-size=1

quarkus.hibernate-orm.database.generation=update
quarkus.hibernate-orm.log.sql=false

# Swagger UI / OpenAPI
quarkus.swagger-ui.always-include=true
quarkus.swagger-ui.path=/swagger-ui
quarkus.smallrye-openapi.path=/openapi

# Encryption key (explicitly configured for StartupLogger)
db.field.encryption.key=${DB_FIELD_ENCRYPTION_KEY:0123456789abcdef0123456789abcdef}

# CORS
quarkus.http.cors=true
quarkus.http.cors.origins=*

# Timezone
mypaybyday.timezone=${TZ:UTC}

# Ollama server configuration
quarkus.langchain4j.ollama.base-url=${OLLAMA_BASE_URL:http://localhost:11434}
quarkus.langchain4j.ollama.chat-model.model-id=${OLLAMA_MODEL_ID:llfm2.5-thinking:1.2b}
quarkus.langchain4j.ollama.timeout=900s
quarkus.langchain4j.ollama.log-requests-curl=false
quarkus.langchain4j.ollama.embedding-model.model-id=${OLLAMA_EMBEDDING_MODEL:nomic-embed-text}
quarkus.langchain4j.ollama.devservices.enabled=false
